# Esp32dev-OTA-WifiManager
Base code for using the ESP32 DevKit's OTA feature, already configured with Wi-Fi Manager to change the Wi-Fi settings without needing to update the code via a cable; includes a 1-minute delay to update the Wi-Fi settings, after which it starts up correctly
